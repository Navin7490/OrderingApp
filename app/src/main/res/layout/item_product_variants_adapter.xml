<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="26dp"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <RadioButton
        android:checked="false"
        android:id="@+id/variantItemRadioButton"
        android:layout_width="0dp"
        android:backgroundTint="@color/app_text_gray"
        android:layout_height="wrap_content"
        android:textColor="@color/app_text_black"
        android:fontFamily="@font/poppins_regular"
        android:textSize="15sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/variantItemPriceTv"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/variantItemPriceTv"
        style="@style/TextAppearance.AppCompat.Widget.TextView.SpinnerItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/start_end_margin"
        android:textSize="15sp"
        android:fontFamily="@font/poppins_regular"
        android:textStyle="normal"
        android:textColor="@color/app_text_gray"
        tools:text="15"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_marginTop="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:visibility="gone"
        android:background="@drawable/drawable_divider"
        app:layout_constraintTop_toBottomOf="@+id/variantItemRadioButton"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>